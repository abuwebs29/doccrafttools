// Replace ONLY these lines in your file:

// BEFORE:
// exportInvoiceAsExcel({ ...form, logoDataUrl });
// exportInvoiceAsWord({ ...form, logoDataUrl });

// AFTER:
exportInvoiceAsExcel(form);
exportInvoiceAsWord(form);
